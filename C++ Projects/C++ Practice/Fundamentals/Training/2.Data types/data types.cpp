#include <iostream>

int main(){
    // Primitive data types
    char yes = 'Y'; // 1 byte
    int x = 1; // 4 bytes
    float y = 1.2f; // 4 bytes - represents a floating point number and it has at least one digit after the floating point. Its represented with f at the end.
    long l = 123123124513421312; // 8 bytes
    double z = 2.333; // 8 bytes - default floating point type
    bool state = true; // represents a state - anything other than 0 means true and 0 means false
    std::string text = "Hello World!"; // a string is a sequence of chars

    return 0;
}